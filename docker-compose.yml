services:
  collabmd:
    image: ${COLLABMD_IMAGE:-collabmd:local}
    depends_on:
      - plantuml
    environment:
      HOST: 0.0.0.0
      PORT: 1234
      PLANTUML_SERVER_URL: http://plantuml:8080
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:1234/health || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    ports:
      - "${COLLABMD_HOST_PORT:-1234}:1234"
    restart: unless-stopped
    volumes:
      - type: bind
        source: ${HOST_VAULT_DIR:-./data/vault}
        target: /data

  plantuml:
    image: plantuml/plantuml-server:jetty
    ports:
      - "127.0.0.1:${PLANTUML_HOST_PORT:-18080}:8080"
    restart: unless-stopped
